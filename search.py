import re
def sexbot(comment_body):
    score = 0
    good = re.findall(comment_body, B)
    bad = re.findall(comment_body, B)
    good_score = len(good)
    bad_score = len(bad)
    
    return(score)









